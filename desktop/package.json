{
  "name": "questrace-desktop",
  "version": "2.0.0",
  "private": true,
  "description": "题迹 Questrace — local-first study companion",
  "author": "Questrace contributors",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.mathnotebook.desktop",
    "productName": "Questrace",
    "files": [
      "main.cjs",
      "external-links.cjs"
    ],
    "extraResources": [
      {
        "from": "resources/",
        "to": "backend",
        "filter": [
          "questrace-server*"
        ]
      }
    ],
    "directories": {
      "output": "../dist/desktop"
    },
    "win": {
      "target": "portable",
      "artifactName": "Questrace-${version}-windows-${arch}.exe"
    },
    "mac": {
      "target": "zip",
      "category": "public.app-category.education",
      "hardenedRuntime": true,
      "identity": "-",
      "notarize": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.inherit.plist",
      "artifactName": "Questrace-${version}-macos-${arch}.zip"
    }
  }
}
